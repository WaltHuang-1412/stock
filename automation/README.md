# Claude Code 自動分析排程系統

## 🎯 概述

這是新一代的自動化系統，直接使用 **Claude Code CLI** 進行排程執行，保持與手動分析完全相同的品質。

## 🆕 與舊版本的差異

| 項目 | 舊版（已移除） | 新版 |
|------|---------------|-----------|
| **執行方式** | Python 腳本 | Claude Code CLI |
| **分析品質** | 機械化邏輯 | ✅ 與手動分析相同 |
| **規範遵循** | 部分遵循 | ✅ 完全遵循 CLAUDE.md |
| **靈活度** | 固定邏輯 | ✅ AI 智能判斷 |
| **錯誤處理** | 容易出錯 | ✅ 智能異常處理 |

## 📅 排程時間

```
🌅 盤前分析：週一至週五 08:30
☀️  盤中分析：週一至週五 12:30
🌆 盤後分析：週一至週五 15:00
```

## 🚀 安裝方式

### 快速安裝
```bash
cd /Users/walter/Documents/GitHub/stock
./automation/install_schedule.sh
```

### 手動安裝
```bash
# 1. 複製排程檔案
cp automation/*.plist ~/Library/LaunchAgents/

# 2. 載入排程
launchctl load ~/Library/LaunchAgents/com.stock.before_market.plist
launchctl load ~/Library/LaunchAgents/com.stock.intraday.plist
launchctl load ~/Library/LaunchAgents/com.stock.after_market.plist
```

## 🔧 管理指令

### 檢查排程狀態
```bash
launchctl list | grep stock
```

### 停止排程
```bash
launchctl unload ~/Library/LaunchAgents/com.stock.*.plist
```

### 重新載入排程
```bash
launchctl load ~/Library/LaunchAgents/com.stock.*.plist
```

### 手動測試
```bash
# 測試盤前分析
claude --print "盤前分析：執行完整市場分析流程"

# 測試盤中分析
claude --print "盤中分析：執行量能掃描和五維度分析"

# 測試盤後分析
claude --print "盤後分析：執行追蹤更新和明日預測"
```

## 📋 日誌管理

### 日誌位置
```
logs/before_market.log      - 盤前分析日誌
logs/intraday.log          - 盤中分析日誌
logs/after_market.log      - 盤後分析日誌
logs/*_error.log           - 錯誤日誌
```

### 查看日誌
```bash
# 最新盤前分析
tail -f logs/before_market.log

# 最新錯誤
tail -f logs/*_error.log

# 今日所有日誌
ls -la logs/ | grep $(date +%Y-%m-%d)
```

## ✅ 優勢

### 1. **品質保證**
- ✅ 使用真正的 Claude Code，非模擬
- ✅ 完全遵循 CLAUDE.md 規範
- ✅ 五維度評分、防止事後諸葛

### 2. **智能處理**
- ✅ 自動應對突發事件（財報、地緣政治）
- ✅ 智能數據驗證
- ✅ 異常狀況自動處理

### 3. **完整追蹤**
- ✅ 自動建立 tracking.json
- ✅ 自動更新追蹤系統
- ✅ 準確率統計

### 4. **日誌完整**
- ✅ 完整執行記錄
- ✅ 錯誤追蹤
- ✅ 效能監控

## ⚠️ 注意事項

### 權限要求
- 確保 Claude Code 有專案目錄存取權限
- 確保 launchd 有執行 Claude Code 權限

### 網路需求
- Claude Code 需要網路連線
- API 呼叫可能有頻率限制

### 錯誤處理
- 如果執行失敗，檢查 error.log
- 網路問題會自動重試
- 嚴重錯誤會記錄到日誌

## 🔄 與手動模式並存

**可以同時使用**：
- 自動排程：處理日常分析
- 手動執行：處理突發狀況、深度討論

**不會衝突**：
- 排程會檢查是否已有當日分析
- 手動分析會覆蓋自動分析
- tracking.json 自動同步

## 📞 故障排除

### 排程未執行
```bash
# 檢查排程是否載入
launchctl list | grep stock

# 檢查排程檔案語法
plutil -lint ~/Library/LaunchAgents/com.stock.*.plist

# 重新載入
launchctl unload ~/Library/LaunchAgents/com.stock.*.plist
launchctl load ~/Library/LaunchAgents/com.stock.*.plist
```

### Claude Code 無法執行
```bash
# 檢查 Claude Code 安裝
which claude

# 檢查權限
claude --print "測試"

# 檢查專案權限
cd /Users/walter/Documents/GitHub/stock && claude --print "測試"
```

### 日誌問題
```bash
# 檢查日誌目錄權限
ls -la logs/

# 手動建立日誌目錄
mkdir -p logs/
chmod 755 logs/
```

---

**更新日期**：2025-11-28
**版本**：1.0
**技術**：Claude Code CLI + macOS launchd