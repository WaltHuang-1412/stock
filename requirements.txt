# 核心依賴
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.3
selenium==4.15.2
pandas==2.1.4
pyyaml==6.0.1
python-dotenv==1.0.0

# AI/LLM
openai==1.3.7

# 新聞解析
feedparser==6.0.10

# P1-1: 新增缺失套件
yfinance==0.2.28          # 股價查詢（指定穩定版本）
pytz==2024.1              # 時區支援（跨平台必須）

# 可選（已內建或視需要安裝）
# urllib3>=1.26.0         # requests 依賴
# certifi>=2023.0.0       # SSL 憑證